CodenamePHP:
  GoogleSecretsManager:
    credentials: 'some credentials'
    project: 'my-project'

    secrets:
      my-simple-secret: "some.path"
      my-complex-secret:
        path: "other.path"
        version: "123"
